[package]
name = "hoshi-test-support"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
tempfile = { workspace = true }

hoshi-control-plane = { path = "../control-plane" }
hoshi-relay = { path = "../relay" }
